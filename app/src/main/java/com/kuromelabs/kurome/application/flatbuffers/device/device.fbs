include "info.fbs";
include "pair.fbs";

namespace kurome.fbs;

enum DeviceQueryType: byte {
    GetSpace,
    GetInfo,
    GetAll
}

table DeviceQuery {
    type: DeviceQueryType;
}

union DeviceResponseType { DeviceInfo } // TODO: add pairing here

table DeviceResponse {
    response:DeviceResponseType;
}